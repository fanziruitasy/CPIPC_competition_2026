# 脚本目录

本目录存放面向使用者的稳定运行入口。每个 Python 脚本必须以中文文件头说明功能、输入和输出，公共函数必须说明 `:param`、`:return` 和可能抛出的异常。

正式入口按职责分为：

- `api`：启动和冒烟检查。
- `ingestion`：来源登记和异步入库。
- `indexing`：统一语料与 Qdrant/DuckDB 快照构建。
- `query`：命令行问答与链路验证。
- `evaluation`：评测集标准化和代表性评测。
- `quality`：文档与表格质量回归。
- `delivery`：七类交付物、镜像和敏感信息核验。
